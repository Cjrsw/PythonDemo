新建一个python项目
在项目终端运行
```
pip install gmssl
```
粘贴代码
```python
from gmssl import sm2, func

sm2_cry = sm2.CryptSM2(
    public_key='fd95025bd0bacf9c62d08f9cda23ad54a2251c62fbb0425e3b3517ebd6c6bbdacb46c7da3eaaf8dcc8277cc6d712c9c6259ef1455eeb8a833224c130f696f35b',  # 填入获取到的公钥
    private_key='', # 私钥不要改，不要动，保持现状
    ecc_table=sm2.default_ecc_table, mode=1
)

plain_text = "R7czTnCHYmpoe36x"  # 明文密码
enc_data = sm2_cry.encrypt(plain_text.encode("utf-8")).hex()
print("密钥是：\n"+enc_data)
```
需要修改的参数（相同无需替换）：
1. public_key，值替换为获取到的公钥
2. plain_text，值替换为需要加密的明文密码
得到的密钥粘贴到登陆接口测试的body的json的password的值即可